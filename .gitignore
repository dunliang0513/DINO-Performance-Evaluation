# Model binaries - large, and the .engine is hardware-specific (Orin Nano, SM 8.7)
*.engine
*.onnx

# Captured data and results
datasets/
results/

# Per-setup artifacts: regenerated by capture_reference.py / select_screws.py,
# specific to one camera + rig, so not meaningful to share
my_photo*.jpg
product_config.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
