[pytest]
# Without this the suite only collects under `python -m pytest` from the repo
# root, which happens to prepend cwd to sys.path. Breaks under bare `pytest`,
# from another directory, or in CI.
pythonpath = .
markers =
    download: test downloads model weights (slow, needs network)
