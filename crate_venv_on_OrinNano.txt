# TensorRT 10.3.0 already installed

python3 -m venv --system-site-packages ~/SIFT_DINO/venv/dino_cmp
source ~/SIFT_DINO/venv/dino_cmp/bin/activate

python -m pip install "numpy==1.24.4"
python -m pip install "torch==2.8.0" "torchvision==0.23.0"
python -m pip install \
    "transformers==5.16.1" \
    "Pillow==10.4.0" \
    onnx \
    pypylon

