# This machine has only Python 3.10.12, so every pin must satisfy
# Requires-Python for cp310 - existing on PyPI is not sufficient.
# scikit-learn and matplotlib are held below their latest releases
# because 1.8+ and 3.11+ respectively require Python >= 3.11.
#
# OpenCV is held at 4.x deliberately: 5.x is newly released and changes
# enough API surface that re-validating the SIFT/FLANN code is not worth it.

--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.9.1+cu128
torchvision==0.24.1+cu128

transformers==5.17.0
timm==1.0.29

opencv-contrib-python==4.12.0.88
numpy==2.2.6
Pillow>=10,<13

# Used by the evaluation plan (phases 5-6); installed now to avoid a second pass
scikit-learn==1.7.2
matplotlib==3.10.9

pytest==9.1.1
